import React from 'react';
import { View } from 'react-native';

import TextComponent from '../TextComponent';

const Intensity = () => {
  return (
    <View>
      <TextComponent>Intensity Component</TextComponent>
    </View>
  );
};

export default Intensity;
